[project]
name = "hypit-image-opencv-runtime"
version = "0.1.0"
description = "Locked Python runtime for the SVML local OpenCV image Provider"
requires-python = ">=3.13,<3.14"
license = { text = "Hypit Open Source License (Apache-2.0 with additional conditions); see LICENSE at the repository root" }
dependencies = [
  "numpy>=2.1,<3",
  "opencv-python-headless>=4.10,<5",
]

[tool.uv]
package = false
