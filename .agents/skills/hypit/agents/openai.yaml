interface:
  display_name: "Hypit"
  short_description: "Build SVML videos and reconstruct reference videos with CLI tools"
  default_prompt: "Use $hypit to author, validate, or refine native SVML video sources; for reference-video reconstruction, run prepare_reference, observe_reference, and inspect_svml_vocabulary before writing complete SVML, SVS, and SVRun sources."
