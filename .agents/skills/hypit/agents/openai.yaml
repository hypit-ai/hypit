interface:
  display_name: "Hypit"
  short_description: "Build SVML videos and reconstruct reference videos with CLI tools"
  default_prompt: "Use $hypit and route from SKILL.md. For reference-video reconstruction, read references/reference-video/index.md and every required child reference in order before running the three CLI commands or authoring sources."
