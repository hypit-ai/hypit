# `@svml/prelude-video`

Ordinary author-package aggregate for the official video language. It contributes Module
Manifests, the SVS Frontend and official Text Surfaces. It has no Provider, credential, store,
queue, network or rendering authority.

The CLI depends only on this replaceable prelude rather than enumerating Script, Film or
HyperFrames packages itself.
