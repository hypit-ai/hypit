# `@hypit/artifact-store-fs`

Filesystem implementation of the Runtime `ArtifactStore` port. It keeps generated bytes under a
content-addressed path and exposes streaming reads, writes and explicit garbage collection.

The Runtime Profile selects this package and supplies its path. It stores no Build state, credentials or
author source. Code embedding the Runtime directly may construct `FileArtifactStore` itself.
