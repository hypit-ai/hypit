# `@svml/protocol`

Data-only v2 bootstrap contracts shared by Core, Drivers and future modules. The package defines
canonical values, module and type references, records and origins, explicit plans, Needs,
Receipts, Derivations, events and commands. It contains no parser, runtime or video vocabulary.
