# `@hypit/preview-mock`

The preview Mock Realizer. It maps compiled Graph outputs to mock Fragments, writes an isolated
temporary `preview.svrun`, and returns preview attachments and provenance with `estimate` timing.
