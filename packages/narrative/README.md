# `@hypit/narrative`

External components use `hypit/narrative` from their `hypit` development dependency. The package
owns the types and helpers below; Source imports retain the `@hypit/narrative@1` Module identity.


Public, provider-neutral contracts for authored speech and semantic selections.
