# `@hypit/program-space`

The provider-neutral time and frame domain shared by a compiled program.

`Clock` is the authored, duration-free frame-rate fact for a video project. It is declared once and
consumed by media normalization and Speech Track; `ProgramSpace` is the later duration-bearing
domain assembled from the semantic Takes.
