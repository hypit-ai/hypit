import { speechEvidenceManifest } from "./index.js";
export const svmlPackage = { format: "svml.node-package@1" as const, name: "@narratage/speech-evidence", modules: [{ manifest: speechEvidenceManifest }] };
export default svmlPackage;
