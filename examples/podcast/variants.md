# Variants

`swap-host`, `swap-item`, and `swap-app` are independent variants of the podcast reference. Each
variant has its own `.svml` author source and matching `.svrun`; shared recipes and kits remain
explicit local SVS inputs.
