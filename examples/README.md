# Examples

`interview/`, `podcast/` and `ranking-football/` are complete video examples. Each includes SVML,
SVS and SVRun sources, project README guidance, assets and accepted generated media; nested
directories are independent variants or explicit reuse projects.

`minimal-author-package/` is a package-authoring fixture, not a video example.
