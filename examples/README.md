# Examples

`interview/`, `podcast/` and `ranking-football/` are complete video examples. Each includes SVML,
SVS and SVRun sources, project README guidance and assets; nested directories are independent
variants or explicit reuse projects.

`minimal-author-package/` is a package-authoring fixture, not a video example.
