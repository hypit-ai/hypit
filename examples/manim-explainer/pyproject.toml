[project]
name = "hypit-manim-showcase"
version = "0.0.0"
description = "Project-local Manim authoring environment for a Taylor expansion explainer"
requires-python = ">=3.11,<3.14"
dependencies = [
  "manim==0.21.0",
]

[tool.uv]
package = false
