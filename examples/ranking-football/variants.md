# Variants

`swap-host`, `swap-effect`, and `swap-topic` are the direct variants. The nested `reference-banana`,
`reference-banana-from-swap`, `swap-effect-banana`, and `swap-effect-banana-reference-sync`
directories are additional independent reuse variants, each with its own SVML/SVS/SVRun sources.
