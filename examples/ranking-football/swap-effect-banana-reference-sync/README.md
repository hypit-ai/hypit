# Banana effect reference-sync variant

An independent reference-synchronized effect variant. Its SVML/SVS/SVRun files and explicit
`accepted/`/`reused/` media inputs are kept together for agent inspection.
