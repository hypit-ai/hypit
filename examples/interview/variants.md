# Variants

`swap-host`, `swap-lang`, and `swap-ride` are independent variants of the street-interview
reference. Each variant has its own `.svml` author source and matching `.svrun`; shared recipes,
kits, assets, and the global `@hypit/interview-emoji-reveal` package remain explicit inputs.
